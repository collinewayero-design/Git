// src/services/geminiService.ts
export const geminiService = {
  generateContent: async (prompt: string) => {
    console.log("Gemini service placeholder for:", prompt);
    return { response: { text: () => "Placeholder response" } };
  }
};
